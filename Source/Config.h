#pragma once

namespace Config
{
    enum MidiType {
        CONTROLCHANGE,
        NOTEON,
        PITCH
    };

    enum ComponentSizes
    {
        FADERWIDTH = 90,
        FADERHEIGHT = 450,
        PADWIDTH = 120,
        PADHEIGHT = 70
    };
}